#!/usr/bin/python3

def new_in_list(my_list, idx, element):
    new_list = my_list[:]
    if idx < o or idx > (len(my_list) - 1)):
        return (my_list)

    new_list[idk] = element
    return (new_list)
